interface:
  display_name: "Ralph Orchestrator"
  short_description: "Repo-specific patterns and workflows for ralph-orchestrator"
  default_prompt: "Use the ralph-orchestrator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true